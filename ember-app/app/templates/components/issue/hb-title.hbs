{{#if canEdit }}
  {{#if isEditing}}
    <button {{action "cancel" }} class="hb-button hb-button-grey">Cancel</button> 
    <button disabled={{disabled}} {{action "save" }} class="hb-button ">Save</button> 
  {{else}}
    <button {{action "edit"  }} class="hb-button hb-button-grey">Edit</button> 
  {{/if}}
{{/if}}

<h2>
  {{#if isEditing }}
    {{input value=bufferedContent.title }}
  {{else}}
    <div class="flex-issue-title">
      <span class="wrap-fix">{{ model.title }}</span>
      <a class="number" href="{{{unbound model.html_url }}}" target="_blank" title="{{model.title}}"><small>#{{model.number}}</small></a>
      {{#if model.linkedColor}}
        <span class="linked-repo" style={{repo-color-style model.linkedColor}}>
          <i class="ui-icon ui-icon-note"></i> {{model.repoName}}
        </span>
      {{/if}}
      {{#if isClosed}}
        <span class="issue-closed-badge"><p> Closed </p></span>
      {{/if}}
    </div>
  {{/if}}
</h2>

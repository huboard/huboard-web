<ul class="nav nav-tabs">
  {{#each panes as |pane|}}
  <li {{attr class="pane.selected:active"}}>
    <a href="#" {{action 'select' pane bubbles=false}}>{{pane.title}}</a>
  </li>
  {{/each}}
</ul>
<div class="tab-content">
  {{yield}}
</div>

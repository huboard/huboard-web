<div class="fullscreen-card">
  <div class="fullscreen-card-preamble clearfix">
    <h3> New Milestone </h3>
  </div>
  <form {{action "submit" on="submit"}} class="flex-form split-form">
    <div class="flex-form-left">
      {{#if errors}}
        <div class="alert alert-error">
          The milestone could not be created! Ensure the title is unique.
        </div>
      {{/if}}
      <label>
        {{input value=model.data.title placeholder="Title"}}
      </label>
      {{hb-markdown-editor repo=model.repo markdown=model.data.description }}
        <button disabled={{disabled}} class="hb-button submit-button">Create milestone</button>
    </div>
    <div class="flex-form-right">
      <h5> 
        Due date: {{moment-format dueDate "L"}} 
        <small> <a href="#" {{action "clearDueDate"}}> clear </a> </small>
      </h5>
      {{hb-date-picker dueDate=model.data.due_on processing=processing }}
    </div>
  </form>
</div>

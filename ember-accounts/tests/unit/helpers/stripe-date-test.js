import { stripeDate } from '../../../helpers/stripe-date';
import { module, test } from 'qunit';

module('Unit | Helper | stripe date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stripeDate(42);
  assert.ok(result);
});

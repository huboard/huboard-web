import { assetPath } from '../../../helpers/asset-path';
import { module, test } from 'qunit';

module('Unit | Helper | asset path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assetPath(42);
  assert.ok(result);
});

import animateModalClose from '../../../utils/animate-modal-close';
import { module, test } from 'qunit';

module('Unit | Utility | animate modal close');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animateModalClose();
  assert.ok(result);
});

import animateModelOpen from '../../../utils/animate-model-open';
import { module, test } from 'qunit';

module('Unit | Utility | animate model open');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animateModelOpen();
  assert.ok(result);
});

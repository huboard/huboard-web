<div class="tabbable">
  <ul class="nav nav-tabs">
  {{#each pane in panes}}
    <li class="{{if pane.selected 'active'}}">
      <a href="#" {{action 'select' pane}}>{{pane.title}}</a>
    </li>
  {{/each}}
  </ul>

  <div class="tab-content">
    {{yield}}
  </div>
</div>
